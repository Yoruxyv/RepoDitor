"""R.E.P.O. installation, local-data, map, and process discovery.

The package establishes trusted game locations and installed-build identity for
read-only metadata consumers and fail-closed safety checks. It does not parse
save semantics or authorize mutations merely because an installation exists.
"""
